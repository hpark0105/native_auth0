import Foundation

enum Auth0Config {
    static let domain = Config.Auth0.domain
    static let clientId = Config.Auth0.clientId
    static let bundleIdentifier = Config.Auth0.bundleIdentifier
    static let callbackURL = Config.Auth0.callbackURL
} 